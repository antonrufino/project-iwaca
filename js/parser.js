(() => {
    angular.module('app')
    .factory('parser', () => {
        return (lexemeTable, symbolTable) => {
            let parser = {}

            // Define parser functions here

            parser.analyze = () => {

            }

            return parser;
        }
    });
})();
